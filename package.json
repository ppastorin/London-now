{
  "name": "london-now",
  "version": "0.3.4",
  "private": true,
  "type": "module",
  "description": "London Now with live data, official departure boards and unified London Advanced tool links.",
  "scripts": {
    "check": "node --check public/app.js && node --check worker/index.js && node scripts/validate.mjs && node --test tests/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "wrangler versions upload"
  },
  "devDependencies": {
    "wrangler": "4.129.0"
  }
}
