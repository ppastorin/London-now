{
  "name": "london-now",
  "version": "0.3.2",
  "private": true,
  "type": "module",
  "description": "London Now with live TfL, Met Office weather and partial airport-access status.",
  "scripts": {
    "check": "node --check public/app.js && node --check worker/index.js && node scripts/validate.mjs && node --test tests/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "wrangler versions upload"
  },
  "devDependencies": {
    "wrangler": "4.129.0"
  }
}
