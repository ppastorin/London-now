{
  "name": "london-now",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "London Now Build 2 with live TfL and Met Office weather.",
  "scripts": {
    "check": "node --check public/app.js && node --check worker/index.js && node scripts/validate.mjs && node --test tests/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "wrangler versions upload"
  },
  "devDependencies": {
    "wrangler": "4.129.0"
  }
}
